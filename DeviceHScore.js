import * as stat from "./stat.js";
const DeviceHealthScore = (id, num) => {
  // Device-SensorHealthScore[id, num] maps device ids to sensor health scores
   return stat.SMA(Device-SensorHealthScore[id, num], num)
}